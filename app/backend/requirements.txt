fastapi
uvicorn[standard]
httpx   # TestClient only; harmless in the image
google-auth[requests]   # Google ID token verification; [requests] is required --
                        # google.auth.transport.requests hard-imports requests
